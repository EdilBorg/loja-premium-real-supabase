import Link from 'next/link';
import { currentUser } from '@/lib/auth';
export default async function Nav(){const u=await currentUser();return <header className="sticky top-0 z-40 border-b border-white/10 bg-[#090613]/85 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link href="/" className="text-xl font-black tracking-tight"><span className="text-rose-400">Loja</span> Premium</Link><nav className="flex items-center gap-2 text-sm"><Link className="rounded-full px-4 py-2 hover:bg-white/10" href="/catalogo">Catálogo</Link>{u?<Link className="rounded-full px-4 py-2 hover:bg-white/10" href={u.role==='CUSTOMER'?'/conta':'/admin'}>{u.role==='CUSTOMER'?'Minha conta':'Painel admin'}</Link>:<><Link className="rounded-full px-4 py-2 hover:bg-white/10" href="/login">Entrar</Link><Link className="btn btn-primary !py-2" href="/criar-conta">Criar conta</Link></>}</nav></div></header>}
