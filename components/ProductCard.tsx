import Link from 'next/link';import { mt } from '@/lib/utils';
export default function ProductCard({p}:{p:any}){return <Link href={`/produto/${p.slug}`} className="group card overflow-hidden"><div className="aspect-[4/3] bg-gradient-to-br from-rose-500/20 to-amber-500/20">{p.coverImage?<img src={p.coverImage} className="h-full w-full object-cover transition group-hover:scale-105"/>:<div className="flex h-full items-center justify-center text-5xl">🛍️</div>}</div><div className="p-5"><div className="mb-2 text-xs font-bold uppercase text-amber-300">{p.category}</div><h3 className="line-clamp-2 text-lg font-black">{p.title}</h3><p className="mt-2 line-clamp-2 text-sm text-white/60">{p.description}</p><div className="mt-4 flex items-center justify-between"><div><strong className="text-xl text-white">{mt(p.price)}</strong>{p.oldPrice?<span className="ml-2 text-sm text-white/40 line-through">{mt(p.oldPrice)}</span>:null}</div><span className="rounded-full bg-white px-4 py-2 text-xs font-black text-slate-950">Ver</span></div></div></Link>}
