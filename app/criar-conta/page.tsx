'use client';import { useActionState } from 'react';import Link from 'next/link';import { registerAction } from '@/app/actions';
export default function Criar(){const [s,a]=useActionState(registerAction,{} as any);return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#f43f5e33,transparent_35%),#080511] px-4"><form action={a} className="card w-full max-w-md p-8"><h1 className="text-3xl font-black">Criar conta</h1><p className="mt-2 text-white/55">Use usuário e senha. O produto comprado ficará salvo aqui.</p>{s?.error?<p className="mt-4 rounded-2xl bg-red-500/20 p-3 text-sm text-red-200">{s.error}</p>:null}<div className="mt-6 space-y-4"><input name="username" className="input" placeholder="Nome de usuário"/><input name="password" type="password" className="input" placeholder="Senha"/><input name="confirm" type="password" className="input" placeholder="Confirmar senha"/><button className="btn btn-primary w-full">Criar conta</button></div><p className="mt-5 text-center text-sm text-white/55">Já tem conta? <Link className="font-bold text-amber-300" href="/login">Entrar</Link></p></form></main>}
