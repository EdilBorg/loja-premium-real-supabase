import Nav from '@/components/Nav';import ProductCard from '@/components/ProductCard';import { prisma } from '@/lib/prisma';
export default async function Catalogo(){const products=await prisma.product.findMany({where:{status:'ACTIVE'},orderBy:{createdAt:'desc'}});return <><Nav/><main className="min-h-screen bg-[#080511] px-4 py-10"><div className="mx-auto max-w-7xl"><h1 className="text-4xl font-black">Catálogo</h1><p className="mt-2 text-white/55">Produtos disponíveis para compra online.</p><div className="mt-8">{products.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{products.map(p=><ProductCard key={p.id} p={p}/>)}</div>:<div className="card p-12 text-center">Nenhum produto disponível.</div>}</div></div></main></>}
