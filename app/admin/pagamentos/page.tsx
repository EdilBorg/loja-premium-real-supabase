import { prisma } from '@/lib/prisma';import { savePaymentSettings } from '@/app/actions';
export default async function Pagamentos(){const s=await prisma.paymentSetting.findFirst();return <div className="p-6 md:p-10"><h1 className="text-4xl font-black">Pagamentos</h1><p className="mt-2 text-white/50">Configure os dados que o cliente verá para pagar manualmente.</p><form action={savePaymentSettings} className="card mt-8 grid gap-5 p-6 md:grid-cols-2"><div><h2 className="mb-4 text-2xl font-black">M-Pesa</h2><input name="mpesaName" defaultValue={s?.mpesaName||''} className="input mb-3" placeholder="Nome da conta M-Pesa"/><input name="mpesaNumber" defaultValue={s?.mpesaNumber||''} className="input" placeholder="Número M-Pesa"/></div><div><h2 className="mb-4 text-2xl font-black">E-Mola</h2><input name="emolaName" defaultValue={s?.emolaName||''} className="input mb-3" placeholder="Nome da conta E-Mola"/><input name="emolaNumber" defaultValue={s?.emolaNumber||''} className="input" placeholder="Número E-Mola"/></div><div><h2 className="mb-4 text-2xl font-black">Banco</h2><input name="bankName" defaultValue={s?.bankName||''} className="input mb-3" placeholder="Nome do banco"/><input name="bankAccount" defaultValue={s?.bankAccount||''} className="input mb-3" placeholder="Número da conta"/><input name="bankHolder" defaultValue={s?.bankHolder||''} className="input" placeholder="Titular"/></div><div><h2 className="mb-4 text-2xl font-black">Contacto</h2><input name="whatsapp" defaultValue={s?.whatsapp||''} className="input" placeholder="WhatsApp de suporte"/></div><button className="btn btn-primary md:col-span-2">Guardar configurações</button></form></div>}
